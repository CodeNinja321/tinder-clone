import React, { useState } from 'react'


function TinderCards() {
    const [people, setPeople] = useStateate([]);
    //const people = [];

    return (
        <div>
            <h1>Tinder Cards</h1>
        </div>
    )
}

export default TinderCards
